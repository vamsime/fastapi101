from typing import List, Tuple, Dict

price: List[int] = [10, 12, 14, 16.2]
print(price)
price: Tuple[int, int, int] = [10, 12, 14]
print(price)
price: Dict[str, int] = {'item1': 23, 'item2': 45}
print(price)